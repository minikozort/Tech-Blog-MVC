<div class="blogs list-group">
  {{#each blogs}}
  <a class="list-item flex-row justify-space-between" href="/mydashboard/edit/{{id}}">
    <span>{{title}}</span>
    <span>{{formatDate createdAt}}</span>
  </a>
  {{/each}}
</div>